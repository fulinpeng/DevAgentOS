# 后端 API 根地址（不要尾斜杠），必须与 apps/backend 的 PORT 一致（默认 3000）
# 若 backend/.env 里写了 PORT=3001，这里改为 http://127.0.0.1:3001
# 前端用 pnpm dev --host 给手机访问时：不能再用 127.0.0.1，应改为本机局域网 IP，例如 http://192.168.1.2:3000
VITE_API_BASE=http://127.0.0.1:3000
